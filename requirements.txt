pandas>=2.2,<3.1
openpyxl>=3.1,<3.2
duckdb>=1.5,<2.0
requests>=2.31,<3.0
beautifulsoup4>=4.12,<5.0
pytest>=8.0,<9.0
PyYAML>=6.0,<7.0
pip-system-certs>=4.0  # makes Python trust the Windows cert store; needed on machines where
                       # antivirus (e.g. Norton) does local HTTPS scanning via its own root CA,
                       # which certifi's bundled list doesn't include. See HANDOFF.md Step 6.
